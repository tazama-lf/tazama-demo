services:
  app:
    container_name: tazama-demo
    image: tazamaorg/demo-ui:v2.0.11-uat
    build:
      context: ./
      target: dev
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
        - .:/app
        - /app/node_modules
        - /app/.next
    ports:
      - "3001:3001"
    restart: always
    networks:
      - network1


networks:
  network1:
    name: tazama_default
    external: true
      